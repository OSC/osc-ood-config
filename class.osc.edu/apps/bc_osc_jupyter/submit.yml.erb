---
batch_connect:
  template: "basic"
  conn_params:
    - jupyter_api
script:
  accounting_id: "<%= account %>"
  native:
    - "--nodes"
    - "1"
    - "--ntasks-per-node"
    - "<%= num_cores %>"
